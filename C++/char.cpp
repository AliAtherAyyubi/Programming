#include<iostream>
#include<fstream>
using namespace std;

int main()
{
    // FILE *fp;
    
    // fp=fopen("text2.text", "r");

    // char ch;

    // while((ch=fgetc(fp))!=EOF){
    //      ch= fgetc(fp);
    //     cout<<ch;
    // }
    // return 0;

    unsigned char a=20;
    cout<<(a<<3);
}